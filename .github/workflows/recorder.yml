name: TikTok 24-7 Auto Recorder

on:
  schedule:
    - cron: '0 */4 * * *'
  workflow_dispatch:

permissions:
  contents: write
  actions: write

jobs:
  run-recorder:
    runs-on: ubuntu-latest
    timeout-minutes: 350

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install FFmpeg
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg

      - name: Install Python Dependencies
        run: |
          pip install -r requirements.txt

      - name: Configure Cloud Credentials
        env:
          GOOGLE_CLIENT_ID: ${{ secrets.GOOGLE_CLIENT_ID }}
          GOOGLE_CLIENT_SECRET: ${{ secrets.GOOGLE_CLIENT_SECRET }}
          GDRIVE_REFRESH_TOKEN: ${{ secrets.GDRIVE_REFRESH_TOKEN }}
          TIKTOK_SESSION_ID: ${{ secrets.TIKTOK_SESSION_ID }}
        run: |
          python -c "
          import os, json
          cfg = {}
          if os.path.exists('config.json'):
              with open('config.json', 'r', encoding='utf-8') as f:
                  cfg = json.load(f)
          if os.environ.get('GOOGLE_CLIENT_ID'):
              cfg['google_client_id'] = os.environ['GOOGLE_CLIENT_ID']
          if os.environ.get('GOOGLE_CLIENT_SECRET'):
              cfg['google_client_secret'] = os.environ['GOOGLE_CLIENT_SECRET']
          if os.environ.get('GDRIVE_REFRESH_TOKEN'):
              cfg['gdrive_refresh_token'] = os.environ['GDRIVE_REFRESH_TOKEN']
              cfg['gdrive_enabled'] = True
          if 'monitored_users' not in cfg:
              cfg['monitored_users'] = ['islizanx', 'itsme_kate0110', 'urielhui38']
          with open('config.json', 'w', encoding='utf-8') as f:
              json.dump(cfg, f, indent=4)

          cookies = {}
          if os.path.exists('cookies.json'):
              with open('cookies.json', 'r', encoding='utf-8') as f:
                  cookies = json.load(f)
          if os.environ.get('TIKTOK_SESSION_ID'):
              cookies['sessionid_ss'] = os.environ['TIKTOK_SESSION_ID']
          with open('cookies.json', 'w', encoding='utf-8') as f:
              json.dump(cookies, f, indent=4)
          print('[✓] Cấu hình credentials hoàn tất.')
          "

      - name: Run TikTok 24-7 Recorder
        run: |
          python -u cloud_daemon.py --duration-minutes 230 --interval 20

      - name: Trigger Next Run (Continuous 24-7 Loop)
        if: always()
        run: |
          curl -X POST \
            -H "Accept: application/vnd.github+v3+json" \
            -H "Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" \
            https://api.github.com/repos/${{ github.repository }}/actions/workflows/recorder.yml/dispatches \
            -d '{"ref":"main"}' || true
